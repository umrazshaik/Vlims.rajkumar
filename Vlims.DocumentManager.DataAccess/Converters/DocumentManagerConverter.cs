//------------------------------------------------------------------------------
// <auto-generated>
//     This code was generated by a tool.
//
//     Changes to this file may cause incorrect behavior and will be lost if
//     the code is regenerated.
// </auto-generated>
//------------------------------------------------------------------------------

namespace Vlims.DocumentManager.DataAccess
{
    using System;
    using System.Text;
    using System.IO;
    using System.Linq;
    using System.Data;
    using System.Collections.Generic;
    using Newtonsoft.Json;
    using Vlims.DMS.Entities;
    using Vlims.Common;



    // Comment
    public static class DocumentManagerConverter
    {
        
        public static List<DocumentManager> SetAllDocumentManager(DataSet dataset)
        {
            try
            {
                List<DocumentManager> result = new List<DocumentManager>();
                DocumentManager documentManagerData;
                if (dataset != null && dataset.Tables.Count > 0 && dataset.Tables[0].Rows.Count > 0)
                {
                    for (int i = 0; (i < dataset.Tables[0].Rows.Count); i = (i + 1))
                    {
                        DataRow row = dataset.Tables[0].Rows[i];
                        documentManagerData = new DocumentManager();
                        documentManagerData.DMGId = Convert.ToString(row[DocumentManagerConstants.DMGId.Trim()]);
                        documentManagerData.Documentrequest = Convert.ToString(row[DocumentManagerConstants.Documentrequest.Trim()]);
                        documentManagerData.documentpreparation = Convert.ToString(row[DocumentManagerConstants.documentpreparation.Trim()]);
                        documentManagerData.DocumentEffective = Convert.ToString(row[DocumentManagerConstants.DocumentEffective.Trim()]);
                        documentManagerData.AdditionalTasks = Convert.ToString(row[DocumentManagerConstants.AdditionalTasks.Trim()]);
                        documentManagerData.CreatedBy = Convert.ToString(row[DocumentManagerConstants.CreatedBy.Trim()]);
                        documentManagerData.CreatedDate = DatatypeConverter.SetDateTime(row[DocumentManagerConstants.CreatedDate.Trim()]);
                        documentManagerData.ModifiedBy = Convert.ToString(row[DocumentManagerConstants.ModifiedBy.Trim()]);
                        documentManagerData.ModifiedDate = DatatypeConverter.SetDateTime(row[DocumentManagerConstants.ModifiedDate.Trim()]);
                        result.Add(documentManagerData);
                    }
                }
                return result;
            }
            catch (System.Exception ex)
            {
                throw;
            }
        }
        
        public static DocumentManager SetDocumentManager(DataSet dataset)
        {
            var result = SetAllDocumentManager(dataset);
            if (result.Count > 0)
            {
                return result.FirstOrDefault();
            }
            return null;
        }
    }
}
