export interface Effective {
  documentTitle: string;
  documentNumber: string;
  documentType: string;
  department: string;
  workflow: string;
  document: string;
  effectiveDate: string;
  reviewDate: string;
}
